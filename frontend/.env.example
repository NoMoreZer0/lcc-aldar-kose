# API Configuration
VITE_API_BASE=/api/v1

# Set to 'false' to use real backend instead of mock data
# Set to anything else (or omit) to use mock mode
VITE_USE_MOCK=false

# Proxy target for development (used in vite.config.ts)
VITE_PROXY_TARGET=http://localhost:8000